{
  "name": "mbti-persona-agent-ui",
  "description": "A local-first MBTI multi-persona ideation workspace with LLM personas and Reddit research mode.",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check llm.js && node --check data.js"
  },
  "engines": {
    "node": ">=18"
  }
}
